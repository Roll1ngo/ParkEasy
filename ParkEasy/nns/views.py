# from django.shortcuts import render
# import cv2  # Для обробки зображень
# from django.http import HttpResponse
#
#
# from .implement_model_detect import get_plate_number_image
#
#
def upload_image(request):
    # if request.method == 'POST':
    #     # form = ImageUploadForm(request.POST, request.FILES)
    #     if form.is_valid():
    #         form.save()
    #         # Шлях до збереженого зображення
    #         image_path = form.instance.image.path
    #         path_to_number_plate = get_plate_number_image(image_path)
    #
    #         return render(request, 'nns/upload_image.html', {'form': form, 'processed_image': path_to_number_plate})
    # else:
    #     form = ImageUploadForm()
    # return render(request, 'nns/upload_image.html', {'form': form})
    pass